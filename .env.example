# Backend used by the Vite development proxy.
VITE_DEV_PROXY_TARGET=https://api.yhy-server.com

# Backend used by production builds when the UI and API are on different origins.
# Leave empty when nginx serves the UI and proxies /api on the same origin.
VITE_API_BASE_URL=https://api.yhy-server.com
