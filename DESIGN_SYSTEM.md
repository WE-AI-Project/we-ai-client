# SynAIpse Client — Design System (As-Implemented)

이 문서는 이상적인 스펙이 아니라 **현재 코드베이스를 스캔해 실제로 쓰이고 있는 스타일**을
기준으로 작성되었습니다. 새 화면/컴포넌트를 만들 때는 하드코딩 값 대신 아래에 정리된
`src/app/colors.ts` / `src/styles/theme.css`의 변수와 Tailwind 클래스를 사용하세요.

> 참고: `design.md`는 초기 기획 단계의 Microsoft Fluent 2 참고 스펙(아이디얼 타겟)이고,
> 이 문서는 그 이후 실제 구현에서 확정된 값(예: border-radius는 Fluent 2보다 더 크고
> 둥근 "Apple 스타일" 스케일로 의도적으로 변경됨)을 기준으로 합니다. 두 문서가 다르면
> **이 문서가 현재 기준**입니다.

---

## Colors

앱의 표준 팔레트는 **다크 네이비 배경 + 인디고/핑크 액센트**입니다 (`src/app/colors.ts`).
`theme.css`의 shadcn 스타일 CSS 변수(`--background`, `--card` 등)가 같은 값을 한 번 더
감싸고 있으며, Tailwind의 `bg-background` 류 유틸리티는 그쪽을 통해 동작합니다.

### 배경 / 서피스

| 변수 | 값 | 용도 |
|---|---|---|
| `OUTER_BG` | `#000000` | 창 최외곽 (프레임리스 윈도우 배경) |
| `CONTENT_BG` / `SIDEBAR_BG` / `TITLEBAR_BG` | `#0A0D3A` | 메인 콘텐츠, 사이드바, 커스텀 타이틀바 |
| `PANEL_BG` | `#1E2353` | 패널, 팝오버 |
| `CARD_BG` / `TABLE_BG` | `#23272A` | 카드, 테이블 |
| `INPUT_BG` | `#1E2353` | 입력 필드 배경 |

### 텍스트

| 변수 | 값 | 용도 |
|---|---|---|
| `TEXT_PRIMARY` → `TEXT_LABEL` | `#1B1F3A` → `#767DA6` | 밝은 배경 위 텍스트 (진함 → 옅음 4단계) |
| `TEXT_ON_DARK` / `TEXT_ON_DARK_MUTED` | `#FFFFFF` / `#B5B9DE` | 어두운 배경(사이드바 등) 위 텍스트 |
| `SIDEBAR_TEXT_ACTIVE` / `SIDEBAR_TEXT` / `SIDEBAR_TEXT_LABEL` | `rgba(255,255,255,0.95/0.55/0.28)` | 사이드바·타이틀바 전용 3단계 |

### 액센트 & 상태색

| 변수 | 값 | 용도 |
|---|---|---|
| `ACCENT` | `#5865F2` (인디고) | 기본 브랜드 액센트, 활성 탭/버튼 |
| `ACCENT_MID` | `#EC48BD` (핑크) | 보조 액센트, 그라디언트 종점 |
| `ACCENT_SAGE` | `#35ED7E` (그린) | 보조 액센트 |
| `UI_GREEN` | `#35ED7E` | 성공 / 활성 / 러닝 상태 |
| `UI_RED` | `#ef4444` | 에러 / 삭제 / 위험 |
| `UI_AMBER` | `#f59e0b` | 경고 / 수정 필요 |
| `UI_VIOLET` | `#EC48BD` | 정보 / 특수 상태 |
| `UI_GRAY` | `#6b7280` | 비활성 / 유휴 |

각 상태색은 `_BG`/`_BG8`/`_BG10` 접미사로 옅은 배경 버전(배지, 뱃지 배경 등)이 함께 정의되어
있습니다. 배지/뱃지 배경을 새로 만들 때는 직접 `rgba()`를 계산하지 말고 이 접미사 버전을
쓰세요.

### 터미널 / 코드 UI

로그 뷰어, diff, 빌드 콘솔은 별도의 **GitHub Dark** 팔레트를 씁니다: `TERM_BG #0d1117`,
`TERM_HEADER #161b22`, `TERM_TEXT #c9d1d9`, `TERM_MUTED #8b949e` 등 (`colors.ts`의
`TERM_*` 상수 전체 참고). 일반 UI 색상과 절대 섞어 쓰지 마세요.

### 규칙

1. 새 hex 값을 인라인으로 쓰지 말고 `colors.ts`에 상수로 추가한 뒤 import해서 쓴다.
2. 기존 상수와 **값이 같은** hex를 발견하면 그 상수로 교체한다 (이번 리팩토링 대상).
3. **알려진 예외 (이번 리팩토링 범위 밖, 별도 진행 예정)**: `#A67B5B`(탄), `#708238`(올리브
   그린), `#5A8A4A`(세이지), `#C09840`(머스타드), `#B85450`(브릭레드), `#888A62`,
   `#6B7A50`, `#62683A`, `#3A3D18` 등 구(舊) 올리브/베이지 테마가 일부 화면
   (`LoginScreen`, `AIQAPage`, `BranchVisualization`, `CalendarPage`, `SkeletonLoader`
   등)에 라이트 배경 전제로 남아있습니다. 이 화면들은 배경·텍스트가 통째로 옅은 톤이라
   값만 새 팔레트로 바꾸면 대비가 깨지므로, 화면 단위로 개별 검토가 필요합니다.
4. **알려진 불일치**: `#10b981`(에메랄드 그린, 53곳)이 공식 성공색 `UI_GREEN(#35ED7E)`과
   다른 값으로 병행 사용 중입니다. 통일 여부는 별도 결정 필요.

---

## Typography

Tailwind 표준 스케일(`text-xs` 12px 등)보다 **한 단계 더 촘촘한 자체 스케일**(임의값
`text-[Npx]`)이 실제 주력입니다 — 정보 밀도가 높은 데스크톱 도구 특성상 의도된 선택입니다.

| 용도 | 크기 | 비고 |
|---|---|---|
| 극소 라벨/배지/타임스탬프 | `text-[8px]` ~ `text-[9px]` | 코드베이스에서 가장 많이 쓰이는 크기 |
| 기본 UI 텍스트(버튼, 필드, 목록, 탭) | `text-[10px]` ~ `text-[11px]` | 두 번째로 많이 쓰이는 크기 |
| 소제목 / 카드 타이틀 | `text-[12px]` ~ `text-sm`(14px) | |
| 섹션 제목 | `text-base`(16px) ~ `text-lg`(18px) | |
| 페이지 타이틀 | `text-xl`(20px) ~ `text-2xl`(24px) | |
| 대형 디스플레이(로그인 등 극히 일부) | `text-[44px]` ~ `text-[52px]` | 예외적, 남용 금지 |

**굵기**: 기본값은 `font-semibold`(전체 사용량의 과반). 강조는 `font-bold`. 일반 문단
본문만 `font-normal`을 쓴다. `font-medium`은 `theme.css`의 `h1~h4`/`label`/`button`
기본 스타일에 이미 지정되어 있어 별도로 지정할 필요가 거의 없다.

**정리 대상**: `text-[9.5px]`, `text-[10.5px]`, `text-[7.5px]`, `text-[8.5px]` 같은 소수점
크기(약 40여 곳)는 의도적 미세조정이 아니라면 인접한 정수 크기로 통일한다.

---

## Spacing & Layout

Tailwind 기본 4px 그리드를 그대로 따르며, 이미 프로젝트 전역에 잘 지켜지고 있습니다
(임의 px 값 사용은 3곳 미만 — 최근 커밋에서 이미 표준화됨. 새 코드도 이 기준을 유지).

| 클래스 | px | 용도 |
|---|---|---|
| `gap-1` / `p-1` | 4px | 아이콘–텍스트 최소 간격 |
| `gap-1.5` / `px-2.5 py-1.5` | 6px | 작은 버튼/칩 내부 여백 |
| `gap-2` / `p-3` | 8 / 12px | 카드 내부 기본 여백, 컨트롤 사이 간격 |
| `gap-3` / `p-4` | 12 / 16px | 카드-카드 간격, 표준 카드 패딩 |
| `p-5` / `p-6` | 20 / 24px | 페이지 컨테이너 여백 |

**규칙**: `p-[13px]` 같은 임의 값 대신 위 표의 가장 가까운 표준 클래스를 쓴다.

---

## Border Radius (Apple-style Squircle Scale)

`src/styles/theme.css`의 `@theme inline` 블록에서 전역 정의됨 — `rounded-*` 클래스만
쓰면 프로젝트 전체에 자동 적용됩니다. 값을 직접 바꾸려면 이 파일 한 곳만 수정하면 됩니다.

| 클래스 | 값 | 용도 |
|---|---|---|
| `rounded-sm` | 8px | 배지, 태그, 체크박스 |
| `rounded-md` | 12px | 인풋, 작은 버튼 |
| `rounded-lg` | 14px | 일반 버튼, 작은 카드 |
| `rounded-xl` | 18px | 큰 카드, 패널, 드롭다운 |
| `rounded-2xl` | 22px | 모달, 대형 카드 |
| `rounded-3xl` | 28px | 최상위 컨테이너, 바텀시트 (희귀) |
| `rounded-full` | — | 아바타, 점, 필 — 원형 전용, 이 스케일과 무관 |

**규칙**: `rounded-[Npx]` 같은 임의 값이나 `style={{ borderRadius: N }}` 하드코딩 대신
위 클래스 또는 `var(--radius-*)`를 쓴다. 완전한 원(50%/999px)과 의도적인 각진 모서리(0)는
예외로 그대로 둔다.

---

## Shadows / Effects

### 유리 효과 (Glassmorphism)

패널/카드/헤더에 표준으로 쓰이는 조합:

```css
background: rgba(255,255,255,0.06~0.16); /* 밝기는 강조 정도에 따라 조절 */
backdrop-filter: blur(12px); /* 사실상 표준값 — 20곳 이상에서 재사용 */
border: 1px solid rgba(255,255,255,0.08~0.14); /* BORDER / BORDER_SUBTLE */
```

### 입체감 그림자 (제안 — 표준화 대상)

현재는 boxShadow 값이 화면마다 제각각(50개 이상의 서로 다른 조합)입니다. 신규 코드부터는
아래 3단계로 통일합니다.

| 단계 | 값 | 용도 |
|---|---|---|
| `shadow-sm` (제안) | `0 2px 8px rgba(0,0,0,0.08)` | 버튼, 작은 카드 hover |
| `shadow-md` (제안) | `0 8px 24px rgba(0,0,0,0.16)` | 패널, 드롭다운, 플로팅 카드 |
| `shadow-lg` (제안) | `0 12px 48px rgba(0,0,0,0.35)` | 모달, 팝오버, 다이얼로그 |

브랜드 컬러가 들어간 "글로우" 그림자(예: `0 18px 42px rgba(88,101,242,0.28)`)는 CTA
버튼/강조 배너 전용 예외로 유지한다.

### 트랜지션

- 기본: `transition-all` (프로젝트 전역 관례 — 성능보다 일관성 우선한 선택)
- 지속시간: `150ms`~`300ms`, 가장 흔한 값은 `200ms`
- 강조 등장 애니메이션(카드 오픈 등)만 `cubic-bezier(0.22, 1, 0.36, 1)` easing 사용
